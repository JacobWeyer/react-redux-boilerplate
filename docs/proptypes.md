#PropTypes
PropTypes exports a range of validators that can be used to make sure the data you receive is valid

```javascript
import PropTypes from 'prop-types';

class Greeting extends React.Component {
  render() {
    return (
      <h1>Hello, {this.props.name}</h1>
    );
  }
}

Greeting.propTypes = {
  name: PropTypes.string
};
```
##PropTypes
###Basic Prop Types
- Any | PropTypes.any | This will just expect literally anything. Preferably you won't use this ever.
- Array | PropTypes.array | This expects an array []
- Boolean | PropTypes.bool | This expects a bool, true/false
- Function | PropTypes.func | This expects a function
- Number | PropTypes.number | This expects a number (integer, decimal, float)
- String | PropTypes.string | This expects a string value 'anything'
- Object | PropTypes.object | This expects an object {}
- Symbol | PropTypes.symbol | This expects a symbol
- Node | PropTypes.node | This expects literally anything that can be rendered (string, number, element, array)
- Element | PropTypes.element | This expects any react Element, ex above you could pass <Greeting />
- Instance Of | PropTypes.instanceOf(Greeting) | This expects a react element that's an instance of the passed value
- One Of | PropTypes.oneOf(['Greeting', 'Test']) | This will make sure your prop is one of these values specifically
- One Of Type | PropTypes.oneOfType([PropTypes.string, PropTypes.number, PropTypes.instanceOf(Message)])
- Array Of | PropTypes.arrayOf(PropTypes.number) | This will make sure your prop is an array of a certain type of node
- Object Of | PropTypes.objectOf(PropTypes.number) | This will make sure your object has values of a certain type
- Object With Shape | PropTypes.shape({ color: PropTypes.string, fontSize: PropTypes.number }) | 
This will let you define the shape of an object

###Required PropTypes
To make any prop type required, where you don't have to set a default prop value, add isRequired to the end of the PropType

```javascript
Greeting.propTypes = {
    name: PropTypes.string.isRequired
}
```

### Custom Prop Types
```javascript
// You can also specify a custom validator. It should return an Error
  // object if the validation fails. Don't `console.warn` or throw, as this
  // won't work inside `oneOfType`.
  customProp: function(props, propName, componentName) {
    if (!/matchme/.test(props[propName])) {
      return new Error(
        'Invalid prop `' + propName + '` supplied to' +
        ' `' + componentName + '`. Validation failed.'
      );
    }
  },

  // You can also supply a custom validator to `arrayOf` and `objectOf`.
  // It should return an Error object if the validation fails. The validator
  // will be called for each key in the array or object. The first two
  // arguments of the validator are the array or object itself, and the
  // current item's key.
  customArrayProp: PropTypes.arrayOf(function(propValue, key, componentName, location, propFullName) {
    if (!/matchme/.test(propValue[key])) {
      return new Error(
        'Invalid prop `' + propFullName + '` supplied to' +
        ' `' + componentName + '`. Validation failed.'
      );
    }
  })
```

##Full Facebook PropTypes Sample
```javascript
MyComponent.propTypes = {
  // You can declare that a prop is a specific JS primitive. By default, these
  // are all optional.
  optionalArray: PropTypes.array,
  optionalBool: PropTypes.bool,
  optionalFunc: PropTypes.func,
  optionalNumber: PropTypes.number,
  optionalObject: PropTypes.object,
  optionalString: PropTypes.string,
  optionalSymbol: PropTypes.symbol,

  // Anything that can be rendered: numbers, strings, elements or an array
  // (or fragment) containing these types.
  optionalNode: PropTypes.node,

  // A React element.
  optionalElement: PropTypes.element,

  // You can also declare that a prop is an instance of a class. This uses
  // JS's instanceof operator.
  optionalMessage: PropTypes.instanceOf(Message),

  // You can ensure that your prop is limited to specific values by treating
  // it as an enum.
  optionalEnum: PropTypes.oneOf(['News', 'Photos']),

  // An object that could be one of many types
  optionalUnion: PropTypes.oneOfType([
    PropTypes.string,
    PropTypes.number,
    PropTypes.instanceOf(Message)
  ]),

  // An array of a certain type
  optionalArrayOf: PropTypes.arrayOf(PropTypes.number),

  // An object with property values of a certain type
  optionalObjectOf: PropTypes.objectOf(PropTypes.number),

  // An object taking on a particular shape
  optionalObjectWithShape: PropTypes.shape({
    color: PropTypes.string,
    fontSize: PropTypes.number
  }),

  // You can chain any of the above with `isRequired` to make sure a warning
  // is shown if the prop isn't provided.
  requiredFunc: PropTypes.func.isRequired,

  // A value of any data type
  requiredAny: PropTypes.any.isRequired,

  // You can also specify a custom validator. It should return an Error
  // object if the validation fails. Don't `console.warn` or throw, as this
  // won't work inside `oneOfType`.
  customProp: function(props, propName, componentName) {
    if (!/matchme/.test(props[propName])) {
      return new Error(
        'Invalid prop `' + propName + '` supplied to' +
        ' `' + componentName + '`. Validation failed.'
      );
    }
  },

  // You can also supply a custom validator to `arrayOf` and `objectOf`.
  // It should return an Error object if the validation fails. The validator
  // will be called for each key in the array or object. The first two
  // arguments of the validator are the array or object itself, and the
  // current item's key.
  customArrayProp: PropTypes.arrayOf(function(propValue, key, componentName, location, propFullName) {
    if (!/matchme/.test(propValue[key])) {
      return new Error(
        'Invalid prop `' + propFullName + '` supplied to' +
        ' `' + componentName + '`. Validation failed.'
      );
    }
  })
};
```
